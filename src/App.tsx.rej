--- src/App.tsx
+++ src/App.tsx
@@ -399,6 +400,14 @@
     onConfirm: (pages: number[]) => void;
   }>({ isOpen: false, title: "", docId: null, pageCount: 0, singleSelection: false, onConfirm: () => {} });

+  const [rotateModalState, setRotateModalState] = useState<{
+    isOpen: boolean;
+    title: string;
+    docId: string | null;
+    pageCount: number;
+    onConfirm: (rotations: Record<number, number>) => void;
+  }>({ isOpen: false, title: "", docId: null, pageCount: 0, onConfirm: () => {} });
+
   // New unified state for visual watermarking
   const [visualWatermarkState, setVisualWatermarkState] = useState<{
     isOpen: boolean;
